version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"
  jobs:
    pre_install:
      - pip install --upgrade pip
    install:
      - pip install .
      - pip install sphinx sphinx_rtd_theme

sphinx:
  configuration: docs/conf.py